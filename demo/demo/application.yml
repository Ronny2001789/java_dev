spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/taskflow
    username: ronnymputla
    password: moronza2001
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: always
